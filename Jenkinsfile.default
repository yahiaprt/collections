pipeline {
    agent {
        docker {
            image 'postman/newman:latest'
            args '-u root --entrypoint='
        }
    }

    stages {
        stage('Run Default Collection') {
            steps {
                sh 'newman run collection.json'
            }
        }
    }

    post {                               
        success {
            build job: 'newman-all-envs', wait: false
        }
    }
}